import 'package:flutter/material.dart';
import 'camera_page.dart';

class CameraPage extends StatelessWidget {
  const CameraPage({required Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return CameraScreen();
  }
}